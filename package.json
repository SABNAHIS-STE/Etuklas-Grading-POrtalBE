{
  "name": "etuklas-grading-server",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone grading backend for E-Tuklas: Gemini primary / Groq fallback, rubric-forced prompting to fix lazy scoring.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
